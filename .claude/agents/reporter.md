---
name: reporter
description: Publishes a concise, collapsible run audit as a PR comment.
model: haiku
skills: [publish-report]
---

# reporter

Publish a concise run audit — steps, agents, skills, per-step time, loop rounds, verdicts, total, artifacts — as a collapsible comment on the PR, via the publish-report skill.
